<launch>
  <arg name="world_name" default="pinky_restaurant.world"/>
  <arg name="model"      default="qwen2.5:7b"/>

  <!-- 1. Gazebo 시뮬레이션 -->
  <include file="$(find-pkg-share pinky_gazebo)/launch/launch_sim.launch.xml">
    <arg name="world_name" value="$(var world_name)"/>
  </include>

  <!-- 2. map → odom 정적 TF (AMCL 없이 SimpleNavigator가 TF를 찾을 수 있도록) -->
  <node pkg="tf2_ros" exec="static_transform_publisher" name="map_to_odom"
        args="0 0 0 0 0 0 map odom">
    <param name="use_sim_time" value="true"/>
  </node>

  <!-- 3. PID 기반 SimpleNavigator (/goal_pose → /cmd_vel) -->
  <include file="$(find-pkg-share pinky_simple_navigator)/launch/simple_drive.launch.xml">
    <arg name="use_sim_time" value="true"/>
  </include>

  <!-- 4. LLM 컨트롤러 (자연어 → /goal_pose) -->
  <include file="$(find-pkg-share pinky_llm_controller)/launch/llm_control.launch.xml">
    <arg name="use_sim_time" value="true"/>
    <arg name="model"        value="$(var model)"/>
  </include>

</launch>
